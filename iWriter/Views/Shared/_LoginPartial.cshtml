@inject SignInManager<iWriterUser> SignInManager
@inject UserManager<iWriterUser> UserManager

    <ul class="navbar-nav">
        @if (SignInManager.IsSignedIn(User))
        {

            /*
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
            </li>
            <li class="nav-item">
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                </form>
            </li>
            */

            <li class="nav-item dropdown">
                <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">
                    Hello @User.Identity.Name!
                    <span class="caret"></span>
                </button>
                <div class="dropdown-menu">
                    <h6 class="dropdown-header">@User.Identity.Name</h6>
                    <div class="dropdown-divider"></div>

                    <a class="dropdown-item" asp-controller="WritingCenter" asp-action="Index"><span class="glyphicon glyphicon-envelope"></span>Account Home</a>
                    <div class="dropdown-divider"></div>

                    <a class="dropdown-item" href="#"><span class="glyphicon glyphicon-envelope"></span>Messages<span class="badge">14 <!--TO-DO--></span></a>
                    <div class="dropdown-divider"></div>

                    <a class="dropdown-item" href="#"><span class="glyphicon glyphicon-envelope"></span>Credits $<span class="badge">0.00 <!--TO-DO--></span></a>
                    <div class="dropdown-divider"></div>

                    <h6 class="dropdown-header">Account Shortcuts</h6>
                    <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Manage Profile</a>
                    <form class="dropdown-item" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                        <button type="submit" class="btn btn-link text-warning bg-danger">Logout</button>
                    </form>
                </div>
            </li>





        }
        else
        {
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
            </li>
        }
    </ul>
    